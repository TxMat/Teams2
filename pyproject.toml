[project]
name = "teams2"
version = "0.1.0"
description = "A WebRTC-based video meeting application with SFU architecture"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9",
    "aiortc>=1.6",
    "av>=12.0",
]

[project.scripts]
teams2 = "main:main"
